kind = "web"
previewPath = "/"
title = "NatiNest"
version = "1.0.0"
id = "artifacts/natinest"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21715

[services.development]
run = "pnpm --filter @workspace/natinest run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/natinest", "run", "build" ]
publicDir = "artifacts/natinest/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21715"
BASE_PATH = "/"
